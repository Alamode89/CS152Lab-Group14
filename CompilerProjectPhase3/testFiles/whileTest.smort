//this file is for testing loops and its conditions and statements within
main 
{
int x = 1;
int y = 100;
int a = 1;
while(y > 0 && x > 0) {
    while(x > 1) {
        if(x == 10) {
            write(x);
        }
        else {
            write(a);
        }
        write(x);
        y = y - 1;
        write(y);
    }
}
}
